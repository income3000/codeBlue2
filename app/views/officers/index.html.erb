<p style="color: green"><%= notice %></p>
<%= link_to 'HOME', root_path %>
<h1>Officers</h1>

<div id="officers">
  <% @officers.each do |officer| %>
    <%= render officer %>
    <p>
      <%= link_to "Show this officer", officer %>
    </p>
  <% end %>
</div>

<%= link_to "New officer", new_officer_path %>
